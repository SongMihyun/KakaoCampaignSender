# kakao-win32

Small Windows helper package for KakaoTalk PC automation.

It contains HWND, foreground-window, clipboard, and low-level Win32 helpers used
by `kakao-pc-driver`. It can also be reused by other Windows automation code.
