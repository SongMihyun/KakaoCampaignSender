# v1.0.24 Beta Release Notes

- 여러 이미지 첨부 시 발생하는 Windows 다중 파일 경고창을 복구 가능한 경고로 처리
- 경고창 확인 후 카카오톡 `파일 전송` 창 등장 여부로 첨부 성공/실패 판별
- 예상 첨부 개수와 `N개 전송` 버튼의 실제 개수 검증 추가
- 파일 전송 창 미감지, 첨부 개수 불일치, 전송 버튼 감지 실패 상태 코드 `4221`~`4223` 추가
- 사용자 중지 시에도 중지 전 첨부 `debug_steps` 보존 보강
